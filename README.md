# Chess Engine (CPU + GPU)

This repository has two engine implementations:

- `cpu/` for the CPU engine
- `gpu/` for the GPU engine

See `cpu/README.md` and `gpu/README.md` for build and run instructions.
